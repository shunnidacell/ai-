Open X bookmarks and collect only X post URLs that are good candidates for Japanese AI news articles.

Important:
- Use the logged-in browser/session if available.
- Go to https://x.com/i/bookmarks or the X bookmarks page.
- Do not search the public timeline. Only use posts that are already bookmarked by the user.
- Do not use curl, grep, or shell scraping against X.
- If the bookmarks page cannot be opened because login is required, return exactly: LOGIN_REQUIRED

Prefer posts about:
- AI agents, AI tools, new models, OSS, MCP, Codex, Claude Code, Cursor, automation, workflows, prompts, and practical AI use cases.
- Japanese posts or posts useful for Japanese readers.
- Posts with concrete steps, examples, screenshots, code, comparisons, or verification.
- Practical posts from individual developers or AI users, not only official announcements.

Exclude:
- Posts unrelated to AI.
- Pure opinions with no useful details.
- Ads or promotion-only posts.
- Duplicate posts about the same topic.
- Posts too short to turn into an article.

Output rules:
- Return only X post URLs.
- One URL per line.
- Maximum 20 URLs.
- No explanations, no numbering, no Markdown, no JSON.

Format:
https://x.com/{account}/status/{id}
